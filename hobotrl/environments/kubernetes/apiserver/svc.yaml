apiVersion: v1
kind: Service
metadata:
  name: ${id}
  labels:
    run: ${id}
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: websocket
  selector:
    run: ${id}